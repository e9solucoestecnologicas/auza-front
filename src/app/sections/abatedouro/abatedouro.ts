import { Component } from '@angular/core';

@Component({
  selector: 'app-abatedouro',
  templateUrl: './abatedouro.html',
  styleUrl: './abatedouro.scss',
})
export class Abatedouro {
  /** Apenas para gerar os itens de lista no wireframe. */
  readonly itens = [0, 1, 2, 3];
}
